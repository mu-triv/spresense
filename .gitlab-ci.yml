build-job:
  variables:
    DEVENV_REPO: devworldsony/spresense-sdk-env:latest
  stage: build
  tags:
    - tvk-home
  script:
    - docker pull $DEVENV_REPO
    - shopt -s expand_aliases
    - alias spresense='docker run --rm -it -u `id -u`:`id -g`  -v '"`pwd`:/spresense -w /spresense/sdk $DEVENV_REPO"
    - type spresense



